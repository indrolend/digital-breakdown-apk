{
  "name": "digital-breakdown-multiplayer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "wrangler types --check && tsc --noEmit && vitest run",
    "test": "vitest run",
    "types": "wrangler types",
    "dev": "wrangler dev",
    "deploy:dry": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.18.5",
    "typescript": "^5.9.3",
    "vitest": "4.1.10",
    "wrangler": "4.111.0"
  }
}
