#pragma once

#include <cstdint>

namespace dbbuild {
inline constexpr const char* GAME_VERSION = "@DB_GAME_VERSION@";
inline constexpr const char* RELEASE_STAGE = "@DB_RELEASE_STAGE@";
inline constexpr const char* SOURCE_COMMIT = "@DB_SOURCE_COMMIT@";
inline constexpr std::uint32_t BUILD_NUMBER = @DB_BUILD_NUMBER@u;
inline constexpr std::uint16_t PROTOCOL_VERSION = @DB_PROTOCOL_VERSION@u;
inline constexpr bool SOURCE_DIRTY = @DB_SOURCE_DIRTY@ != 0;
}
