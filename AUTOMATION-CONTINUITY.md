# Digital Breakdown Automation Continuity

Treat this ZIP as the sole current authority. Reconstruct by extracting `HEAD.zip`, applying `WORKING-TREE.patch`, then overlaying `UNTRACKED/` at matching paths. Do not restart from GitHub or an older snapshot unless this package is corrupt.

Preserve the causal chain:
environment -> EnemyPerception -> animal intention / EnemyMotor -> locomotion intention -> PhysicalEnemyBody -> contacts/world -> actual body state -> visual presentation.

Current connected arc includes physical foot/COM support, visible weight transfer, wet traction and scramble/recovery, uncertain environmental investigation, perception-bound herd behavior, shared rain attenuation of nonvisual cues, room-pressure atmosphere, physical foot contact deforming existing grass, and dirt/contact presentation consuming those same authoritative foot plants.

Each future run should inspect actual code, choose one highest-leverage unresolved relationship, make one coherent bounded change, test the externally meaningful contract, run strict compilation where available plus `git diff --check`, then emit a new ZIP in this same format. Prefer convergence over parallel managers. Do not invent a rigid roadmap.

Full Windows/OpenGL runtime verification remains environment-dependent and must not be claimed unless actually performed.

Latest relationship: active vacuum noise now contributes its existing audible strength and vacuum pull-point location to EnemyPerception's uncertain environmental cue. Animals can investigate where the phone action occurred without learning hidden player truth; rain/exposure still owns cue attenuation.

- physical loss of footing/disruption -> weather-attenuated spatial herd cue -> uncertain investigation.
- competing environmental cues -> short-memory persistence: weaker cues tug an existing investigation belief rather than teleporting attention; substantially stronger evidence can redirect it.
- room history/heat -> slower decay of unresolved EnemyPerception belief, increasing persistence without creating evidence, precision, or confirmation.
- room history/heat -> unresolved EnemyPerception vigilance -> larger bounded gaze search, making persistent uncertainty visibly readable without adding evidence or locomotion authority.
- dirt presentation convergence -> one shared deterministic three-lobe footprint now owns both visible dirt and contact membership; patches are contiguous, lower-opacity, and preset density drops before identity on Legacy.

- physical recovery convergence -> one posture spring now owns both prone settling and get-up. The previous simultaneous prone spring + upright recovery torque had a reproducible nonzero equilibrium that could leave `fallen` true forever; settled fallen bodies now retarget the same spring upright and regain locomotor authority only after actual pitch/roll recover.

- startup daylight convergence -> exposed rooms now initialize the existing 180-second weather clock at its daylight peak (45 s / phase 0.25). Previously the first gameplay update overwrote default daylight 1.0 with 0.5, dimming sky/ambient/key/fill immediately and reading as a dark fullscreen film. No overlay or brightness compensation was added.

- weather-lighting authority convergence -> the desktop renderer's existing daylight/rain transformation now lives in `render_contract::weatherLightingProfile`, so startup exposure and night/rain composition are contract-testable without duplicating renderer math. Numerical presentation is unchanged; this hardens the startup-dark-film correction against regression.

- room-pressure lighting authority convergence -> the renderer's remaining private room-pressure ambient/fill/key/fog math now lives in `render_contract::roomPressureLightingProfile`. `SceneResponse.roomPressure` remains the sole pressure fact; the renderer only consumes the contract. Numerical presentation is unchanged and the cleared-room release is now tested end-to-end at the contract level.

- floor-material authority convergence -> existing room setting + goal progress + authoritative weather wetness/exposure now flow through `MaterialResponse::floorMaterialResponse` before rendering. The desktop renderer no longer privately owns floor darkening/specular math; numerical presentation is preserved, Legacy still retains wet color identity while optional specular remains preset-gated.

## Latest pass: Multi-Body Vegetation Convergence
Grass now preserves two simultaneous authoritative enemy foot-contact disturbances instead of allowing the strongest animal to erase nearby physical presence. Continue from this full authority tree; prefer another measured relationship/convergence pass over new subsystem creation.

## Merge-readiness stabilization
The complete authority now has a release-visible verification boundary. Twenty focused physicality, perception, herd, lighting, and dirt contracts are registered in the default native CTest suite with assertions active in Release builds. Source-inspection contracts run from the repository root instead of depending on an accidental shell working directory. The multiplayer relay now shares native protocol version 10, matching the expanded replicated world snapshot and gameplay version 8. Do not lower the native protocol to paper over mismatches.

Windows storefront configuration, build, staged identity, staged smoke, strict C++20 `Game.cpp` compilation, the 59-test native suite, the 13-test relay suite, protocol consistency, room-inspector smoke, and production dependency audit pass. Public release is still gated on the repository's required macOS/Linux artifact jobs, aggregate manifest verification, real-hardware graphics/controller acceptance, and final asset-license/attribution review.
